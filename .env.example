# Django
DEBUG=False
SECRET_KEY=tu-secret-key-super-segura-aqui-cambiar-en-produccion
DJANGO_SETTINGS_MODULE=flash.settings.prod

# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host.neon.tech/dbname?sslmode=require

# Allowed Hosts (separados por coma)
ALLOWED_HOSTS=localhost,127.0.0.1,.onrender.com

# URLs
SITE_URL=https://tu-app.onrender.com

# Archivos estáticos y media
STATIC_ROOT=/app/staticfiles
MEDIA_ROOT=/app/media

# Email Configuration (Gmail SMTP)
# Para desarrollo: EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
# Para producción: EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=ivanandreshernandezc@gmail.com
EMAIL_HOST_PASSWORD=tu-password-de-aplicacion-de-gmail
DEFAULT_FROM_EMAIL=ivanandreshernandezc@gmail.com

# 📧 IMPORTANTE: Configurar Gmail App Password
# 1. Ir a: https://myaccount.google.com/apppasswords
# 2. Crear una contraseña de aplicación para "Mail"
# 3. Copiar la contraseña generada (16 caracteres sin espacios)
# 4. Pegar en EMAIL_HOST_PASSWORD

# Stripe (si usas pagos)
STRIPE_PUBLIC_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
